# Auto-detect text files
* text=auto

# Force LF for shell scripts (critical for Linux)
*.sh text eol=lf
*.py text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf

# Windows-only files can keep CRLF
*.bat text eol=crlf
